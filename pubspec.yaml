name: student_helper
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  # Use to get Google fonts
  google_fonts: ^2.1.0
  google_maps_flutter: ^2.1.1
  firebase_core: ^2.8.0
  firebase_auth: ^4.3.0
  cloud_firestore:
  get: ^4.6.5
  authentication_repository: ^2.0.0+3
  geolocator: ^9.0.2
  location: ^4.4.0
  http:
  google_maps_webservice: ^0.0.19
  flutter_polyline_points: ^1.0.0
  splashscreen:
  google_sign_in:
  geocoding:
  flutter_email_sender: ^5.2.0
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
flutter:


  uses-material-design: true

  assets:
    - assets/


